<%- if bundler? -%>
require 'yard'
YARD::Rake::YardocTask.new  
<%- else -%>
begin
  gem 'yard', '<%= @development_dependencies['yard'] %>'
  require 'yard'

  YARD::Rake::YardocTask.new  
rescue LoadError => e
  task :yard do
    warn e.message
  end
end
<%- end -%>
task :doc => :yard
