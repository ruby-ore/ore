<%- if bundler? -%>
require 'rubygems/tasks'
Gem::Tasks.new
<%- else -%>
begin
  require 'rubygems/tasks'

  Gem::Tasks.new
rescue LoadError => e
  warn e.message
  warn "Run `gem install rubygems-tasks` to install Gem::Tasks."
end
<%- end -%>
